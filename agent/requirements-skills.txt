# In-container report-skill Python deps (installed into the runtime image, on top
# of requirements.txt). Pinned to versions validated locally.
reportlab==5.0.0
openpyxl==3.1.5
pypdf==6.14.2
pandas==3.0.3
lxml==6.1.1
pillow==12.3.0
